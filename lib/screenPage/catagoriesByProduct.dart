import 'package:flutter/material.dart';

class catagoriesByProduct extends StatelessWidget {
  const catagoriesByProduct({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Catagories Product'),),
      body: Center(),
    );
  }
}